---
import "../styles/global.css";

interface Props {
  title?: string;
}
const { title = "HABIT QUEST" } = Astro.props;
console.log(
  "SSR Check - Supabase URL exists:",
  !!import.meta.env.PUBLIC_SUPABASE_URL,
);
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, viewport-fit=cover"
    />
    <title>{title}</title>
    <!-- Favicon would go here -->
  </head>
  <body class="crt selection:bg-quest-purple selection:text-white">
    <slot />
  </body>
</html>
